#include "main.h"
/**
 * _abs - absolute value of integer
 * @n: the input 
 * Return: n absolute value
 */

int _abs(int)
{
	int n;

	if (n >= 0)
	{
		return (n);
	}
	if else (n < 0)
		return (n * -1);
	}
}
