#include "main.h"
/**
 * _abs - absolute value of integer
 * @n: the input integer
 * Return: n absolute value
 */

int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else (n < 0)
	{	
		return (n * -1);
	}
}

